<% @posts.each do |post| %>
<div class="post wrapper">
  <h2 class="title"><%= link_to post.title, post %></h2>
  <h3 class="body"> <%= post.body.truncate(300) %>
  <p class="date"><%= post.created_at.strftime("%B, %d, %Y") %>
</p>
</div>
<% end %>